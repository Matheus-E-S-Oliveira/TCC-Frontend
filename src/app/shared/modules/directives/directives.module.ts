import { NgModule } from '@angular/core';
import { DynamicMaskDirective } from '../../models/structure/directives/dynamicMaskDirective/dynamic-mask.directive';

@NgModule({
  declarations: [
    DynamicMaskDirective
  ],
  imports: [
  ],
  exports: [
    DynamicMaskDirective
  ]
})
export class DirectivesModule { }
